import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.madeelshabbir.ionic',
  appName: 'angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
